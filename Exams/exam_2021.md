# AIA — Exam 2021

## Question 1 — Classical Computer Vision Pipeline

**1 point**

Bring the steps of the **Classical Computer Vision Pipeline** into the correct order.

> **Note:** The six draggable steps are not contained in the text export.

---

## Question 2 — Classical Computer Vision

**1 point**

Which of the following statements are true?

* [ ] **a.** An intensity histogram feature is rotation invariant.
* [ ] **b.** An intensity histogram feature is illumination invariant.
* [ ] **c.** The feature extraction step maps from pixel space into the output space.
* [ ] **d.** The bag-of-words feature describes spatial relations between image patches.

---

## Question 3 — Computer Vision Tasks

**1 point**

The company ICU has an automatic access system to ensure that only employees can access their building. A person wishing to enter the building has to show their face to a camera. The acquired image is then matched against an internal database of images of employee faces.

This system is an example application of the general computer vision task of:

* [ ] **a.** Object localization
* [ ] **b.** Object captioning
* [ ] **c.** Semantic segmentation
* [ ] **d.** Object recognition

---

## Question 4 — Fourier Descriptor Normalization

**2 points**

Given is the following Fourier descriptor:

> **Note:** The descriptor figure is missing from the text export.

The following images show different types of normalization applied to the original signal.

Assign to each image the corresponding **invariance** that has been enforced.

> The x-axis shows the frequency \(\mu\). Also note that the frequency range has been shifted.

> **Note:** The four images are missing from the text export.

---

## Question 5 — Fourier Descriptors

**1 point**

Let

$$
F(\mu), \qquad \mu \in \{0,1,\ldots,69\}
$$

be the Fourier descriptor of some contour.

It is known that

$$
F(0)=343+292i.
$$

How many points were used to describe the contour?

**Answer:** __________

---

## Question 6 — Fourier Descriptor Invariances

**2 points**

Let \(F\) be a Fourier descriptor. We apply the following transformations and obtain the normalized descriptor \(\tilde F\):

$$
\tilde F(0) := 0
$$

$$
\tilde F(\mu) := \frac{F(\mu)}{|F(2)|}.
$$

Which of the following invariances are enforced by this normalization?

* [ ] **a.** \(\tilde F\) is invariant to translation.
* [ ] **b.** \(\tilde F\) is invariant to scale.
* [ ] **c.** \(\tilde F\) is invariant to mirroring.
* [ ] **d.** \(\tilde F\) is invariant to noise.

---

## Question 7 — Hough Transform

**1 point**

Given is an image showing the Hough voting space for line detection, containing **three maxima**.

> **Note:** The image is missing from the text export.

Which geometric object(s) could cause this?

* [ ] **a.** Triangle
* [ ] **b.** Square
* [ ] **c.** Parallelogram
* [ ] **d.** Circle

---

## Question 8 — Hough Line Parameterization

**2 points**

Let

$$
(x,y)=(3,7)
$$

be a 2D point.

Which of the following lines parameterized by \((\theta,\rho)\) intersect this point?

* [ ] **a.** \((\theta,\rho)=(0.5\pi,3)\)
* [ ] **b.** \((\theta,\rho)=(0,7)\)
* [ ] **c.** \((\theta,\rho)=(0,3)\)
* [ ] **d.** \((\theta,\rho)=(0.75\pi,-7)\)

---

## Question 9 — Generalized Hough Transform / R-Table

**2 points**

Given is the following R-table of a template:

| Orientation \(\theta\) | Displacement     |
| ---------------------- | ---------------- |
| \([0,45)\)             | \((1,2); (3,4)\) |
| \([45,90)\)            |                  |
| \([90,135)\)           | \((5,6); (7,8)\) |
| \([135,180)\)          | \((9,10)\)       |

What is the corresponding R-table with scale

$$
s=2
$$

and a counter-clockwise rotation of

$$
\theta=180^\circ?
$$

### a.

| Orientation \(\theta\) | Displacement     |
| ---------------------- | ---------------- |
| \([0,45)\)             | \((1,2); (3,4)\) |
| \([45,90)\)            |                  |
| \([90,135)\)           | \((5,6); (7,8)\) |
| \([135,180)\)          | \((9,10)\)       |

### b.

| Orientation \(\theta\) | Displacement             |
| ---------------------- | ------------------------ |
| \([0,45)\)             | \((-2,-4); (-6,-8)\)     |
| \([45,90)\)            |                          |
| \([90,135)\)           | \((-10,-12); (-14,-16)\) |
| \([135,180)\)          | \((-18,-20)\)            |

### c.

| Orientation \(\theta\) | Displacement     |
| ---------------------- | ---------------- |
| \([0,45)\)             | \((5,6); (7,8)\) |
| \([45,90)\)            | \((9,10)\)       |
| \([90,135)\)           | \((1,2); (3,4)\) |
| \([135,180)\)          |                  |

### d.

| Orientation \(\theta\) | Displacement         |
| ---------------------- | -------------------- |
| \([0,45)\)             | \((4,2); (8,6)\)     |
| \([45,90)\)            |                      |
| \([90,135)\)           | \((12,10); (16,14)\) |
| \([135,180)\)          | \((20,18)\)          |

---

## Question 10 — Generalized Hough Transform

**1 point**

Which statements about the **Generalized Hough Transform (GHT)** are true?

* [ ] **a.** R-tables can deal with partial occlusions.
* [ ] **b.** Translating the template object does not change the R-table.
* [ ] **c.** In order to apply R-tables at a position \((x,y)\), one needs to calculate the orientation of the corresponding gradients.
* [ ] **d.** A single R-table is invariant to scale and rotation of the template.

---

## Question 11 — Machine Learning Fundamentals

**1 point**

Which of the following statements are true?

* [ ] **a.** We can measure the degree of overfitting using the training data.
* [ ] **b.** Using validation data is only necessary if the error function is non-convex.
* [ ] **c.** The number of model parameters in logistic regression depends on the number of classes and the input dimensionality.
* [ ] **d.** Gradient descent is only necessary if the error function is non-convex.

---

## Question 12 — Transfer Learning

**1 point**

It is common practice to use networks pre-trained on an unrelated dataset for the task at hand.

Why does such an initialization often lead to improved results compared with a random initialization of the network parameters?

* [ ] **a.** Because low-level perceptive capabilities represented in the layers closer to the input are useful across different tasks.
* [ ] **b.** Because transfer learning reduces the number of model parameters at test time.
* [ ] **c.** Because pre-training requires all layers except the final output layer to remain frozen during fine-tuning, thereby preventing overfitting entirely.
* [ ] **d.** Because high-level decision strategies represented in the layers closer to the output layers can be reused.

---

## Question 13 — Machine Learning

**1 point**

Which of the following statements are true?

* [ ] **a.** Logistic regression, in contrast to linear regression, allows for non-linear classification boundaries.
* [ ] **b.** Data augmentation has a regularization effect.
* [ ] **c.** Model variance can be measured by fitting the model to different subsets of the data.
* [ ] **d.** Optimizing the parameters of a CNN with stochastic gradient descent always leads to the optimal solution.

---

## Question 14 — Backpropagation

**1 point**

Which of the following statements are true?

* [ ] **a.** Backpropagation allows for efficient optimization of computational graphs with gradient descent.
* [ ] **b.** Addition nodes backpropagate their inputs.
* [ ] **c.** Backpropagation reduces noise when using stochastic gradient descent.
* [ ] **d.** Optimizing the parameters of a CNN with backpropagation always leads to the optimal solution.

---

## Question 15 — Backpropagation

**1 point**

Which of the following statements are true?

* [ ] **a.** For every update step, we need multiple forward passes through the network.
* [ ] **b.** The memory consumption of backpropagation is proportional to the combined output dimensionalities of the nodes in the network.
* [ ] **c.** For every update step, we need multiple backward passes through the network.
* [ ] **d.** The memory consumption of backpropagation is proportional to the number of trainable parameters in the network.

---

> **Question 16 is not present in the supplied export.**

---

## Question 17 — Deformable Parts Model

**1 point**

How are objects, or their parts, localized within images in the **Deformable Parts Model**, when using a dense feature space such as HOG?

* [ ] **a.** Using Hough forest regression.
* [ ] **b.** Classifying patches at multiple locations.
* [ ] **c.** The output of the SVM is a location.
* [ ] **d.** Using a codebook and Hough voting.

---

## Question 18 — Implicit Shape Model

**1 point**

Bring the steps of the **training phase of the Implicit Shape Model** into the correct order.

> **Note:** The five draggable steps are not contained in the text export.

---

## Question 19 — Convolutional Layers

**1 point**

The input tensor to a convolutional layer has dimensionality

$$
(134,134,6).
$$

The output tensor has dimensionality

$$
(67,67,12).
$$

The filter size is

$$
(3,3).
$$

What is the **stride** of the layer?

**Answer:** __________

---

## Question 20 — Pooling

**1 point**

What are effects of **max-pooling layers** in Convolutional Neural Networks?

* [ ] **a.** Loss of spatial/relational information.
* [ ] **b.** Dimensionality reduction.
* [ ] **c.** Translation equivariance.
* [ ] **d.** Translation invariance.

---

## Question 21 — Convolutional Layer Parameters

**1 point**

The input tensor to a convolutional layer has dimensionality

$$
(H\times W\times C)=(100,100,12).
$$

The output tensor has dimensionality

$$
(H'\times W'\times C')=(50,50,18).
$$

The filter size is

$$
(3,3).
$$

How many trainable parameters does the layer have, considering **only the kernel tensor and no bias**?

**Answer:** __________

---

## Question 22 — Convolutional Filters

**1 point**

What is the result of an increased number of filters in a convolutional layer of a Convolutional Neural Network?

* [ ] **a.** Higher abstraction capabilities of the layer.
* [ ] **b.** Increase in output dimensionality of the layer.
* [ ] **c.** Decrease in output dimensionality of the layer.
* [ ] **d.** Increased number of features the layer can potentially respond to.

---

## Question 23 — Histograms and Entropy

**1 point**

Given are four image patches.

Drag and drop the matching **intensity histograms** and **entropy values** to the correct image patch.

Available items:

* Histogram a
* Histogram b
* Histogram c
* Histogram d
* Entropy a
* Entropy b
* Entropy c
* Entropy d

> **Note:** The image patches, histograms, and entropy values are missing from the text export.

---

## Question 24 — Saliency

**1 point**

Which is the **most salient image patch**, given the entropy values above and using the method by **Kadir et al.** introduced in the lecture?

* [ ] **a.** d
* [ ] **b.** b
* [ ] **c.** a
* [ ] **d.** c

---

## Question 25 — Entropy and Saliency

**1 point**

Why is the entropy of the distribution of intensity values alone **not sufficient as a measure of saliency**?

* [ ] **a.** Saliency can only be measured in color images.
* [ ] **b.** Because regions with a single intensity value are salient.
* [ ] **c.** Uniformly distributed intensity values always lead to high entropy.
* [ ] **d.** The distribution of intensity values does not contain spatial information.

---

## Question 26 — Sequential Models

**2 points**

Given is the following one-hot encoding:

$$
\text{"0"} \mapsto (1,0,0)
$$

$$
\text{"1"} \mapsto (0,1,0)
$$

$$
EOS \mapsto (0,0,1)
$$

A sequential model \(f\) has been trained using this encoding.

During inference, the model generates the following outputs:

| Input      | Output              |
| ---------- | ------------------- |
| \(f(x_1)\) | \((0.1,0.8,0.1)\)   |
| \(f(x_2)\) | \((0.9,0.05,0.05)\) |
| \(f(x_3)\) | \((0.6,0.4,0.0)\)   |
| \(f(x_4)\) | \((0.2,0.7,0.1)\)   |
| \(f(x_5)\) | \((0.1,0.3,0.6)\)   |

What is the corresponding sequence?

$$
\_\_\_,\quad \_\_\_,\quad \_\_\_,\quad \_\_\_,\quad \_\_\_
$$

---

## Question 27 — Backpropagation Through Time

**1 point**

Given is the RNN shown below.

What is the derivative with respect to \(x_1\) if you apply **backpropagation through time** for the sequence

$$
(x_1,x_2,x_3)?
$$

Assume

$$
a=1,
$$

$$
x_1=3,\qquad x_2=4,\qquad x_3=3,
$$

and the initial state

$$
h_0=0.
$$

> **Note:** The RNN diagram is missing from the text export.

**Answer:** __________

---

## Question 28 — Probability Density Estimation

**1 point**

Which of the following models/methods estimate a distribution over the data?

* [ ] **a.** Parzen Windows
* [ ] **b.** Generative Adversarial Networks
* [ ] **c.** Maximum likelihood estimation
* [ ] **d.** (Restricted) Boltzmann Machines

---

## Question 29 — Maximum Likelihood Estimation

**3 points**

Assume a computer vision system is observing an industrial production process for errors.

We measure the number of errors in a certain time interval. Let

$$
X=\{2,3,8,6,4\}
$$

be the measurements.

We model the probability of observing \(k\) errors using a **Poisson distribution**:

$$
P(k)=\frac{\lambda^k}{k!}e^{-\lambda}.
$$

What is the maximum likelihood estimate

$$
\lambda_{\mathrm{MLE}}
$$

given the measurements?

> Rounding the solution to two decimal places is sufficient.

**Answer:** __________

---

## Question 30 — Bayesian Inference

**3 points**

Two probabilistic models are given by their respective parameters

$$
\theta\in\{\Omega_1,\Omega_2\}.
$$

The likelihoods of these models on data \(D\) are

$$
P(D\mid\theta=\Omega_1)=0.9
$$

and

$$
P(D\mid\theta=\Omega_2)=0.43.
$$

Additionally, we have the prior probabilities

$$
P(\theta=\Omega_1)=0.72
$$

and

$$
P(\theta=\Omega_2)=0.28.
$$

What is the posterior probability

$$
P(\theta=\Omega_1\mid D)?
$$

> Round to four decimal places.

**Answer:** __________

---

## Question 31 — Discriminant Functions

**1 point**

Which of the following are **discriminant functions**?

* [ ] **a.**

  $$
  g_i(x)=P(\omega_i)
  $$

* [ ] **b.**

  $$
  g_i(x)=\sqrt{p(x\mid\omega_i)P(\omega_i)}
  $$

* [ ] **c.**

  $$
  g_i(x)=P(\omega_i\mid x)
  $$

* [ ] **d.**

  $$
  g_i(x)=p(x\mid\omega_i)
  $$

---

## Question 32 — Generative Adversarial Networks

**1 point**

Which of the following statements about **Generative Adversarial Networks (GANs)** are true?

* [ ] **a.** All GANs require a labeled dataset, i.e. a dataset consisting of tuples \((x,y)\), where \(x\) is the input and \(y\) is the desired output.
* [ ] **b.** CycleGANs train two generators and two discriminator networks simultaneously.
* [ ] **c.** The generator network learns the empirical probability distribution of the dataset.
* [ ] **d.** The computation of the backpropagation for the generator network depends on the discriminator.

---

## Question 33 — Generative Models and Self-Supervised Learning

**1 point**

Which of the following statements are true?

* [ ] **a.** Variational autoencoders learn an approximation of the data distribution.
* [ ] **b.** The goal of energy-based SSL is to map images with small mean-square distance in pixel space to the same region of the representation space.
* [ ] **c.** Self-supervision objectives are necessarily regression tasks.
* [ ] **d.** Contrastive learning uses data augmentation to generate a gradient.

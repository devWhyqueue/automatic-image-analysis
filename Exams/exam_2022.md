# AIA — Test Exam 2022

## Question 1 — Computer Vision Tasks

**1 point**

A company is developing software to automatically analyze basketball matches.

The system provides bounding boxes around players and identifies each player's name and number. Additionally, it describes what the player is doing, e.g. dribbling, throwing, defending, etc.

This system is an application of:

* [ ] **a.** Object Classification
* [ ] **b.** Object Recognition
* [ ] **c.** Semantic Segmentation
* [ ] **d.** Action Recognition

---

## Question 2 — Computer Vision Processing Chain

**1 point**

Given is a dataset of images for an image segmentation task. Since the images were taken under different illumination conditions, we apply a transformation such that all their histograms have approximately the same distribution. As a result, all images have a similar color scheme.

Which part of the typical computer vision processing chain is described by this method?

* [ ] **a.** Feature Extraction
* [ ] **b.** Feature Preprocessing
* [ ] **c.** Data Preprocessing
* [ ] **d.** Postprocessing

---

## Question 3 — From Classification to Detection

**1 point**

Given is an image classifier that can classify objects for a specific image resolution.

Which of the following steps are usually used to extend the model for **object detection**?

* [ ] **a.** Computation of bounding boxes based on image scale and model response
* [ ] **b.** Use image pyramids or a similar method to rescale input images to different resolutions
* [ ] **c.** Apply the model to images using a sliding-window approach
* [ ] **d.** Non-maxima suppression of model responses

---

## Question 4 — Computer Vision Processing Chain

**1 point**

Which of the following statements about the computer vision processing chain are true?

* [ ] **a.** Deep learning methods can combine all or most steps in the processing chain into a single differentiable model.
* [ ] **b.** Neural networks can be used as classifiers.
* [ ] **c.** Neural networks can be used as feature extractors.
* [ ] **d.** Neural networks can be used for feature selection.

---

## Question 5 — Entropy

**1 point**

Which statements about entropy are true?

* [ ] **a.** A noisy image patch with a histogram describing a uniform distribution

  $$
  p(I_i)=\frac{1}{255}
  \qquad
  \forall i=0,\ldots,255
  $$

  has the highest possible entropy value.

* [ ] **b.** Let \(p(I_1)\) and \(p(I_2)\) be the histogram values corresponding to two intensity values. A color transformation that switches both colors with each other does not affect the entropy value of the image.

* [ ] **c.** A permutation of pixels changes the entropy of that image patch.

* [ ] **d.** A homogeneous image patch has higher entropy than an image patch containing an edge.

---

## Question 6 — Gradient Correlation and R-Tables

**1 point**

In the exercises, we learned that we can use correlation of the complex gradients of the image

$$
I_I
=
\frac{\partial}{\partial x}I(x,y)
+
i\frac{\partial}{\partial y}I(x,y)
$$

and the template

$$
O_I
=
\frac{\partial}{\partial x}O(x,y)
+
i\frac{\partial}{\partial y}O(x,y)
$$

instead of R-tables.

Which statements are true about this method?

* [ ] **a.**

  $$
  \operatorname{Re}(I_I \star O_I)(x,y)
  $$

  computes the sum of dot products between gradient vectors at a given position \((x,y)\), which is almost equivalent to Hough voting.

* [ ] **b.**

  $$
  \operatorname{Im}(I_I \star O_I)(x,y)
  $$

  computes the sum of dot products between gradient vectors at a given position \((x,y)\), which is almost equivalent to Hough voting.

* [ ] **c.** Correlation can be computed using the Fourier transform as

  $$
  I_I\star O_I
  =
  \operatorname{DFT}^{-1}
  \left(
  \operatorname{DFT}(I)^*
  \operatorname{DFT}(O)^*
  \right).
  $$

* [ ] **d.** Correlation can be computed using the Fourier transform as

  $$
  I_I\star O_I
  =
  \operatorname{DFT}^{-1}
  \left(
  \operatorname{DFT}(I)\cdot
  \operatorname{DFT}(O)
  \right).
  $$

---

## Question 7 — R-Table Transformation

**2 points**

Given is the following R-table of a template:

| Orientation \(\theta\) | Displacement     |
| ---------------------- | ---------------- |
| \([0,90)\)             | \((1,2); (3,4)\) |
| \([90,180)\)           |                  |
| \([180,270)\)          | \((5,6); (7,8)\) |
| \([270,360)\)          | \((9,10)\)       |

What is the corresponding R-table with scale

$$
s=1,
$$

a counter-clockwise rotation of

$$
\theta=180^\circ,
$$

and a translation of the template by

$$
t=(10,10)?
$$

### a.

| Orientation \(\theta\) | Displacement     |
| ---------------------- | ---------------- |
| \([0,90)\)             | \((1,2); (3,4)\) |
| \([90,180)\)           |                  |
| \([180,270)\)          | \((5,6); (7,8)\) |
| \([270,360)\)          | \((9,10)\)       |

### b.

| Orientation \(\theta\) | Displacement         |
| ---------------------- | -------------------- |
| \([0,90)\)             | \((-5,-6); (-7,-8)\) |
| \([90,180)\)           | \((-9,-10)\)         |
| \([180,270)\)          | \((-1,-2); (-3,-4)\) |
| \([270,360)\)          |                      |

### c.

| Orientation \(\theta\) | Displacement     |
| ---------------------- | ---------------- |
| \([0,90)\)             | \((5,6); (7,8)\) |
| \([90,180)\)           | \((9,10)\)       |
| \([180,270)\)          | \((1,2); (3,4)\) |
| \([270,360)\)          |                  |

### d.

| Orientation \(\theta\) | Displacement     |
| ---------------------- | ---------------- |
| \([0,90)\)             | \((5,4); (3,2)\) |
| \([90,180)\)           | \((1,0)\)        |
| \([180,270)\)          | \((9,8); (7,6)\) |
| \([270,360)\)          |                  |

---

## Question 8 — Hough Line Detection

**1 point**

Assume we are building a Hough line detector to find lines parameterized by

$$
(\cos\theta,\sin\theta)(x,y)^T=\rho.
$$

We discretize the voting space as

$$
\theta\in\{0^\circ,1^\circ,2^\circ,\ldots,359^\circ\}
$$

and

$$
\rho\in\{-100,-99,\ldots,0,\ldots,99,100\}.
$$

Given a point \((x,y)\) and its gradient

$$
(G_x(x,y),G_y(x,y)),
$$

how many tuples

$$
(\theta_i,\rho_i)
$$

are potential line candidates for the point with the given information?

Equivalently: **How many votes are cast by this point?**

* [ ] **a.** \(360\)
* [ ] **b.** \(201\)
* [ ] **c.** \(1\)
* [ ] **d.** \(360\times201\)

---

## Question 9 — R-Tables

**1 point**

Which statements about R-tables are true?

* [ ] **a.** A single R-table is invariant to translation.
* [ ] **b.** A single R-table is robust to noise.
* [ ] **c.** A single R-table is invariant to scale.
* [ ] **d.** A single R-table is invariant to rotation.

---

## Question 10 — Hough Voting Spaces

**2 points**

Given are images of four Hough line voting spaces.

What are the corresponding shapes?

1. ---
2. ---
3. ---
4. ---

> **Note:** The four voting-space images are missing from the text export.

---

## Question 11 — Maximum Likelihood Estimation

**3 points**

Assume a computer vision system is observing an industrial production process for errors.

On any given day, there is a probability \(p\) of a production failure.

We measure the number of failure-free days until the first failure occurs. Let

$$
D=\{8,6,10,9,4\}
$$

be these measurements.

We model the probability that a production failure happens on day \(k\) using a **geometric distribution**:

$$
P(k)=(1-p)^{k-1}p.
$$

What is the maximum likelihood estimate

$$
p_{\mathrm{MLE}}
$$

given the measurements?

> Round to two decimal places.

**Answer:** __________

---

## Question 12 — Bayesian Decision Theory and Risk

**2 points**

A company is building a diagnostic system. Given a measurement \(x\), it calculates the patient's probability of having a certain disease:

$$
P(\omega_i\mid x),
\qquad
\omega_i\in\{\omega_1,\omega_2\}.
$$

* \(\omega_1\): patient is ill
* \(\omega_2\): patient is healthy

There are two possible actions:

* \(\alpha_1\): treat the patient
* \(\alpha_2\): do nothing

Each action has an associated risk

$$
\lambda(\alpha_i\mid\omega_j).
$$

The risk table in the original export is represented as:

```text
        α1   α2
ω1      18   57
ω2       2   70
```

Let

$$
P(\omega_1\mid x)=0.75
$$

be the estimated probability that a specific patient is ill.

What is the **expected risk associated with applying the treatment** to this patient?

> Round to two decimal places.

**Answer:** __________

---

## Question 13 — Bayesian Decision Theory

**1 point**

Which of the following statements are true for Bayesian decision theory?

* [ ] **a.** If all classes have Gaussian distributions with the same covariance matrix \(\Sigma\),

  $$
  p(x\mid\omega_i)\sim\mathcal N(\mu_i,\Sigma)
  \qquad \forall i,
  $$

  then the decision boundary between two classes is linear.

* [ ] **b.** If all classes have arbitrary Gaussian distributions,

  $$
  p(x\mid\omega_i)\sim\mathcal N(\mu_i,\Sigma_i)
  \qquad \forall i,
  $$

  then the decision boundary between two classes can be described by a hyperquadric.

* [ ] **c.** If all classes have Gaussian distributions with the same mean \(\mu\),

  $$
  p(x\mid\omega_i)\sim\mathcal N(\mu,\Sigma_i)
  \qquad \forall i,
  $$

  then

  $$
  g_i(x)=P(\omega_i)
  $$

  is a valid discriminant function.

* [ ] **d.** Given a binary classification problem where both classes have the same Gaussian distribution,

  $$
  p(x\mid\omega_i)\sim\mathcal N(\mu,\Sigma)
  \qquad \forall i,
  $$

  and additionally

  $$
  P(\omega_2)>P(\omega_1),
  $$

  then

  $$
  g_1(x)=1,
  \qquad
  g_2(x)=2
  $$

  are valid discriminant functions.

---

## Question 14 — Bayesian Decision Theory

**1 point**

Which of the following statements about Bayesian decision theory are true?

* [ ] **a.** If the optimal decision boundary between classes is linear, then the corresponding class distributions must be Gaussian.

* [ ] **b.** If

  $$
  g_i(x)
  $$

  is a discriminant function, then

  $$
  \frac{1}{g_i(x)}
  $$

  is also a discriminant function.

* [ ] **c.** If all classes have equal prior probability,

  $$
  P(\omega_i)=P(\omega_j)
  \qquad \forall i,j,
  $$

  then

  $$
  g_i(x):=
  \frac{p(x\mid\omega_i)}{p(x)}
  $$

  are valid discriminant functions.

* [ ] **d.** If all classes have the same class-conditional distribution,

  $$
  p(x\mid\omega_i)=p(x\mid\omega_j)
  \qquad \forall i,j,
  $$

  then

  $$
  g_i(x):=P(\omega_i)
  $$

  are valid discriminant functions.

---

## Question 15 — Bayes Classification Error

**1 point**

Given is a binary classification problem. We select the class with the highest posterior probability:

$$
\max\left[P(\omega_1\mid x),P(\omega_2\mid x)\right].
$$

Which of the following statements are true?

* [ ] **a.** The expected classification error is

  $$
  \int_{-\infty}^{\infty}
  \min\left[
  P(\omega_1\mid x),
  P(\omega_2\mid x)
  \right]
  p(x)\,dx.
  $$

* [ ] **b.** The expected classification error is

  $$
  \int_{-\infty}^{\infty}
  \max\left[
  P(\omega_1\mid x),
  P(\omega_2\mid x)
  \right]
  p(x)\,dx.
  $$

* [ ] **c.** If

  $$
  P(\omega_1\mid x)>P(\omega_2\mid x)
  \qquad \forall x,
  $$

  then the expected classification error is

  $$
  P(\omega_2).
  $$

* [ ] **d.** If

  $$
  P(\omega_1\mid x)>P(\omega_2\mid x)
  \qquad \forall x,
  $$

  then the expected classification error is

  $$
  P(\omega_2\mid x).
  $$

---

##
